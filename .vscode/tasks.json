{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build library",
            "command": "cmake",
            "args": [
                "--build ${workspaceRoot}\\build",
                "--config Debug",
                "--target all"
            ]
        }
    ]
}